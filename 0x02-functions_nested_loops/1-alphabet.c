#include "main.h"

/**
 * Print_alphabet - Prints alphabet in lowercase from a to z
 * Return: 0 Success
*/

void print_alphabet(void)
{
int alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
_putchar(alpha);
_putchar('\n');
}
